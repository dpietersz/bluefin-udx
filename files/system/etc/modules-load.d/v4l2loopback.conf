# Autoload v4l2loopback at boot so OBS Studio's "Start Virtual Camera"
# button works without a manual `sudo modprobe v4l2loopback` first.
# Module options live in /etc/modprobe.d/v4l2loopback.conf.
v4l2loopback
